"""Entrypoint stub for the Loyalty & Re-engagement agent.

See this folder's CLAUDE.md for the full spec (inactivity detection,
scheduled win-back sends, opt-out enforcement). No business logic yet --
this is a structure-only scaffold built on top of packages/agent-core
(mielikkix_agent_core) once that package is real.
"""


def main() -> None:
    raise NotImplementedError(
        "Loyalty & Re-engagement agent is not implemented yet. See "
        "CLAUDE.md in this folder for the spec."
    )


if __name__ == "__main__":
    main()
