{
  "name": "@mielikkix/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI components for apps/dashboard (and apps/chat-widget's own UI).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo \"packages/ui: no build configured yet (structure-only scaffold)\" && exit 0"
  }
}
