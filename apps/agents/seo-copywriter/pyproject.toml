[project]
name = "mielikkix-agent-seo-copywriter"
version = "0.0.0"
description = "SEO Copywriter — bulk-generates product descriptions and SEO metadata, with a draft/approve flow before touching live data. Queued agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
