[project]
name = "mielikkix-agent-template"
version = "0.0.0"
description = "Template for a Mielikkix Force agent. Copy this folder to apps/agents/<agent-name>/ to start a new agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
