[project]
name = "mielikkix-agent-support-triage"
version = "0.0.0"
description = "Support Triage — sorts inbound chat/voice-handoff tickets, drafts replies, escalates to a human when needed. Powers website/'s live chat widget. Flagship agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
