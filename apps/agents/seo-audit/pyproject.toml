[project]
name = "mielikkix-agent-seo-audit"
version = "0.0.0"
description = "SEO Audit & Optimization — technical/on-page site audits plus the original SEO Copywriter (bulk product descriptions and SEO metadata), with a draft/approve flow before touching live data. Built and live."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
