# infra/docker

Placeholder — see [`../README.md`](../README.md#docker). Each app's
Dockerfile currently still lives alongside it
(`apps/api/Dockerfile`, `apps/dashboard/Dockerfile`); consolidating them here
is a future step.
