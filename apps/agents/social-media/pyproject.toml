[project]
name = "mielikkix-agent-social-media"
version = "0.0.0"
description = "Social Media Agent — turns a plain-language business update into ready-to-post social captions, for human review before posting. Queued agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
