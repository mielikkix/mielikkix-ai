[project]
name = "mielikkix-agent-quote-invoice"
version = "0.0.0"
description = "Quote & Invoice — turns a customer request into a formal quote, then an invoice on acceptance. Queued agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
