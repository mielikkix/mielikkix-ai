[project]
name = "mielikkix-agent-review-reputation"
version = "0.0.0"
description = "Review & Reputation — monitors a tenant's Google/Facebook reviews, drafts AI reply suggestions, optionally auto-posts within guardrails. Flagship agent (Day 6)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
