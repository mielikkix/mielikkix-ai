[project]
name = "mielikkix-agent-feedback-survey"
version = "0.0.0"
description = "Feedback & Survey — sends post-booking/ticket surveys and summarizes responses into plain-language sentiment summaries. Queued agent."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
